apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "icc-webui.fullname" . }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: icc-webui.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "icc-webui.fullname" . }}
                port:
                  number: {{ .Values.service.port }}

